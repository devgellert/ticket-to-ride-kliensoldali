const imgWidth = 800;
const imgHeight = 544;
const onePercentWidth = imgWidth / 100;
const onePercentHeight = imgHeight / 100;

export default {
  imgHeight,
  imgWidth,
  onePercentHeight,
  onePercentWidth,
};
